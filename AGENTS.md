# Agent notes

- Org labels: `c/` · `t/` · `i/` · `a/` — see KolektivComputer/.github Docs/Issue Labels.md
- Package scope: `@kolektiv/*`
- JS/TS Vite-shaped work: prefer Vite+ (`vp`) for commands; on NixOS prefer Nix for Node/tooling (not `vp env`)
- Always spell **Kascade** with the K
